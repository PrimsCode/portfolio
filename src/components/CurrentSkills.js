import React from "react";
import {Grid, Container, Paper, Typography, Button} from '@mui/material';

const CurrentSkills = () => {
    return (
        <Grid margin={10} align="center" justifyContent="center" alignItems="center">
            <Grid>
                <Typography variant="h2">Current SKills</Typography>
            </Grid>
        </Grid>
    )
}

export default CurrentSkills;